# Copy to config.env and source before running ingest
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER=grc-review

# Graph app registration needs Application permission User.Read.All (admin consent)

# Optional: local-only demo (no blob)
# DRY_RUN=true
