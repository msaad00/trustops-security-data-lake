AWSTemplateFormatVersion: "2010-09-09"
Description: TrustOps read-only AWS posture role for IAM evidence collection.

Parameters:
  TrustedPrincipalArn:
    Type: String
    Description: ARN of the AWS principal allowed to assume this role.
    AllowedPattern: "^arn:aws[a-zA-Z-]*:iam::[0-9]{12}:(role|user|root)(/.+)?$"
  ExternalId:
    Type: String
    Description: External ID condition for cross-account assumption.
    MinLength: 8
    MaxLength: 128
    AllowedPattern: "^[A-Za-z0-9+=,.@:/_-]+$"
    NoEcho: true
  RoleName:
    Type: String
    Description: Name for the TrustOps read-only posture role.
    Default: TrustOpsPostureReadOnlyRole
    AllowedPattern: "^[A-Za-z0-9+=,.@_-]{1,64}$"

Resources:
  TrustOpsPostureReadOnlyRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName:
        Ref: RoleName
      Description: Read-only role for TrustOps AWS IAM posture evidence collection.
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: TrustOpsAssumeRole
            Effect: Allow
            Principal:
              AWS:
                Ref: TrustedPrincipalArn
            Action: sts:AssumeRole
            Condition:
              StringEquals:
                sts:ExternalId:
                  Ref: ExternalId
      Policies:
        - PolicyName: TrustOpsPostureReadOnly
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: TrustOpsIamPostureReadOnly
                Effect: Allow
                Action:
                  - iam:GetAccountPasswordPolicy
                  - iam:GetAccountSummary
                  - iam:ListMFADevices
                  - iam:ListUsers
                Resource: "*"

Outputs:
  RoleArn:
    Description: ARN to configure as the AWS posture connector's read-only role.
    Value:
      Fn::GetAtt:
        - TrustOpsPostureReadOnlyRole
        - Arn
