AWSTemplateFormatVersion: "2010-09-09"
Description: TrustOps hosted connector runtime role assumed by the deployed TrustOps workload.

Parameters:
  TrustedWorkloadPrincipalArn:
    Type: String
    Description: ECS task role, EKS IRSA role, EC2 role, or federated workload principal running TrustOps.
    AllowedPattern: "^arn:aws[a-zA-Z-]*:iam::[0-9]{12}:(role|root)(/.+)?$"
  CustomerRoleName:
    Type: String
    Default: TrustOpsPostureReadOnlyRole
    AllowedPattern: "^[A-Za-z0-9+=,.@_-]{1,64}$"

Resources:
  TrustOpsConnectorRuntimeRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: TrustOpsConnectorRuntimeRole
      Description: Runtime identity used by TrustOps to assume customer-owned read-only connector roles.
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              AWS:
                Ref: TrustedWorkloadPrincipalArn
            Action: sts:AssumeRole
      Policies:
        - PolicyName: AssumeCustomerPostureRoles
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: sts:AssumeRole
                Resource:
                  Fn::Sub: "arn:${AWS::Partition}:iam::*:role/${CustomerRoleName}"

Outputs:
  RuntimeRoleArn:
    Description: Configure this value as TRUSTOPS_AWS_LINK_PRINCIPAL.
    Value:
      Fn::GetAtt:
        - TrustOpsConnectorRuntimeRole
        - Arn
